source 'https://rubygems.org'

# External dependencies
gem 'dotenv', '~> 2.8'     # For loading environment variables from .env file
gem 'fileutils', '~> 1.7'  # For file and directory operations
gem 'json', '~> 2.6'       # For JSON parsing and generation
gem 'logger', '~> 1.5'     # For logging capabilities
gem 'colorize', '~> 0.8'   # For colorized terminal output

# Development and debugging tools
group :development do
  gem 'pry', '~> 0.14'     # For interactive debugging
end
